name: CI
on:
  push:
    branches:
      - 'main'
    tags:
      - 'v*'
    paths-ignore:
      - data/words.ljson
      - data/words.idx
      - CHANGELOG.md
  pull_request:

env:
  # Pin until Node 24.16.0's extract-zip/yauzl regression is fixed. It can
  # leave WebdriverIO's ChromeDriver download partially extracted in CI:
  # https://github.com/nodejs/node/issues/63487
  NODE_VERSION: 24.15.0

jobs:
  test:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    name: Test

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Install pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 11

      - name: Setup node
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      # Check the build works. No need to run the build:edge target since it's
      # almost identical to build:chrome
      - name: Build
        run: |-
          pnpm install --frozen-lockfile
          pnpm build:firefox
          pnpm build:chrome
          pnpm build:thunderbird

      # The runner image only provides Firefox as a snap (/snap/bin/firefox).
      # Snaps get a private /tmp, so Firefox can't read the temporary profile
      # geckodriver passes it via -profile and the WebDriver session never
      # starts, making the browser (firefox) tests time out. Install an
      # unconfined Firefox and put it ahead of the snap on the PATH.
      - name: Setup Firefox
        uses: browser-actions/setup-firefox@v1

      - name: Run tests
        env:
          CI: true
        run: |-
          which -a firefox
          pnpm test

      - name: Upload any failure artifacts
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: test-screenshots
          path: '**/__screenshots__/**/*.png'
          if-no-files-found: ignore
          retention-days: 20

      # Check packaging doesn't error. Again, no need to worry about the
      # package:edge target.
      - name: Package
        run: |-
          pnpm package:firefox
          pnpm package:chrome

      - name: Verify cosmos setup
        run: pnpm cosmos-export

  lint:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    name: Lint

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Install pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 11

      - name: Setup node
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm oxlint --type-aware scripts src tests "*.{cjs,js,ts}"

      - name: Check formatting
        run: pnpm prettier --check _locales css scripts src tests "*.{js,json,md,ts}"

      - name: knip
        run: pnpm knip
