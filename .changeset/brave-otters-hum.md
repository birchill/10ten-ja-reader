---
'10ten-ja-reader': minor
---

Added an experimental, off-by-default setting (Settings → Audio) that adds a
button to play a word's readings as audio, with a karaoke-style overlay
highlighting each mora as it's spoken. Playing audio sends the displayed
reading (and any kanji) to 10ten's audio server to fetch the clip. Toggle
playback with the `p` shortcut.
